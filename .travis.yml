{
  "language": "generic",
  "os": [
    "linux"
  ],
  "dist": "xenial",
  "sudo": true,
  "services": [
    "docker"
  ],
  "before_install": [
    "docker build -t arediez/docker2 -f Dockerfile.dev ."
  ],
  "script": [
    "docker run -e CI=true arediez/docker2 npm run test"
  ]
}